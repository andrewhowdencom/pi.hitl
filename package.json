{
  "name": "pi.hitl",
  "version": "0.2.0",
  "description": "CEL-based permission sandbox for the pi coding agent",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "bin": {
    "pi-hitl-validate": "./validate.ts"
  },
  "scripts": {
    "test": "tsx --test *.test.ts",
    "validate": "tsx validate.ts",
    "validate:example": "tsx validate.ts .pi/nope.yaml .pi/permissions.test.yaml"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "typebox": "*"
  },
  "dependencies": {
    "@bufbuild/cel": "^0.4.0",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
